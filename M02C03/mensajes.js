//Cristian Jara - Comision 15 - 2022

module.exports = ( mensaje ) => {
	let divisor = `\r\n${'-'.repeat( 40 )}\r\n`;
	console.log( divisor + mensaje );
	console.log( 'Las acciones disponibles son: listar' + divisor );
}